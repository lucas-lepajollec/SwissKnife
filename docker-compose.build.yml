# Construction locale depuis ce dépôt (contributeurs).
# Usage : docker compose -f docker-compose.yml -f docker-compose.build.yml up -d --build

services:
  swissknife:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_APP_TITLE: SwissKnife
    image: swissknife:local
